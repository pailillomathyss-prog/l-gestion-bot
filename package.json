{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "echo 'Build skipped - Bot reset'",
    "start": "cd artifacts/api-server && node src/index.js"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
